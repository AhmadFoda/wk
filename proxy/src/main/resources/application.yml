server:
  port: 8080

spring:
  application:
    name: proxy
  cloud:
    gateway:
      routes:
        - id: backend
          uri: http://backend:8080
          predicates:
            - Path=/api/**
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com